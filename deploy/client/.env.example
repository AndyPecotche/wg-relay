# NO versionar este archivo: contiene el token.
WGRELAY_TOKEN=wgr_...

# Versión de la imagen del agente. Mientras el código viva en la rama v1, el
# tag "latest" no existe: usá v1 (o el número de release, ej. 0.1.0).
WGRELAY_VERSION=v1
